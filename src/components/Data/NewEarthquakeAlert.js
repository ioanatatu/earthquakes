// import css from './NewEarthquakeAlert.module.scss';

import React, { useState, useEffect } from 'react';
import PropTypes from 'prop-types';

/**
 * NewEarthquakeAlert component receives data and maps it for WordCloud and Metadata props
 *
 * @param {string} title The title given to the DataCard.
 * @param {array} data Array of topics.
 */
const NewEarthquakeAlert = (props) => {
   return <div>Alert</div>;
};

// NewEarthquakeAlert.propTypes = {
//    title: PropTypes.string,
//    data: PropTypes.array,
//    getParam: PropTypes.func,
// };

export default NewEarthquakeAlert;
